# Secret scope is created by install/bootstrap.py (DABs do not declare secret scopes).
# This file is a placeholder so resources/ has a coherent layout. Real secret values
# (jwt_signing_key, bootstrap_admin_token, optional anthropic/openai keys) are
# managed via `databricks secrets put-secret --scope foreman --key <key>`.
